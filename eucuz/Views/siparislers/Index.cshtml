@model IEnumerable<eucuz.Models.siparisler>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_adminLayout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.urun_Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.adet)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.urun_Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.adet)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.siparis_Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.siparis_Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.siparis_Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
